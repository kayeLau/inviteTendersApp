<view class="xl-popup-container" wx:if="{{visiable}}" bind:tap="closePopup">
  <view class="card xl-popup">
    <view class="xl-popup-header">
      <text class="font-bold">{{title}}</text>
      <text>X</text>
    </view>
    <slot></slot>
  </view>
</view>